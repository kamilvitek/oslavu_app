{
  "scripts": {
    "setup": "npm install && echo 'Setup complete! Next steps:' && echo '1. Set up your .env.local file with API keys' && echo '2. Set up Supabase database' && echo '3. Run npm run dev to start development'",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "db:generate": "supabase gen types typescript --local > src/types/supabase.ts",
    "db:reset": "supabase db reset",
    "db:migrate": "supabase db push"
  }
}